/// Which country, and which larger regions, a place is in, from
/// country-coder's borders.
///
/// Use it with `package:osm/osm.dart`, and with `package:osm/editor.dart`,
/// whose editor and presets use it to know which kinds of thing apply where.
library;

export 'src/country_coder.dart';
export 'src/country_coder_cache.dart';
